[package]
description = "Linux platform mediator for PlatynUI — delegates to X11 or Wayland sub-platform at runtime"
name = "platynui-platform-linux"
version.workspace = true
keywords.workspace = true
categories.workspace = true
edition.workspace = true
homepage.workspace = true
documentation.workspace = true
repository.workspace = true
authors.workspace = true
license.workspace = true
rust-version.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
platynui-core = { path = "../core" }
platynui-platform-linux-x11 = { path = "../platform-linux-x11" }
platynui-platform-linux-wayland = { path = "../platform-linux-wayland" }
inventory = "0.3"
tracing = { version = "0.1", default-features = false, features = ["std"] }

[target.'cfg(target_os = "linux")'.dev-dependencies]
rstest = "0.26"

[lints]
workspace = true
