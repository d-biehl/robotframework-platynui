# Tasks

## 1. Prerequisite and namespace harness

- [ ] 1.1 Confirm `fix-compositor-foreign-pidns-clients` has landed before anything else: start the compositor, connect a Wayland client from a sibling PID namespace, map a window, then send `list_windows` over the control socket, and verify the compositor is still alive afterwards. On an unfixed compositor this kills the whole session (`wayland-backend-0.3.15/src/rs/server_impl/client.rs:314` via `window_pid`, `apps/wayland-compositor/src/control.rs:1270-1278`). Do not start task 4 until this passes.
- [ ] 1.2 Add a local namespace harness under `scripts/` that starts the compositor headless inside `unshare --user --map-current-user --keep-caps --pid --fork --mount-proc` with its Wayland and control sockets in a shared directory, and runs a given command inside a second, sibling namespace with `WAYLAND_DISPLAY`, `PLATYNUI_CONTROL_SOCKET` and `XDG_RUNTIME_DIR` pointing at that directory. Verify the harness really reproduces the boundary: `SO_PEERCRED` on the Wayland socket reports a non-zero PID from inside the compositor's namespace and `0` from the sibling one. No container runtime, no X server.
- [ ] 1.3 Add a `just` recipe that runs the harness (name it alongside the existing `test-*` recipes; it is local-only, like `test-java-agent-live`) and document in its comment that it needs `kernel.apparmor_restrict_unprivileged_userns=0` on distributions that restrict unprivileged user namespaces. Verify the recipe runs the harness end to end and fails loudly when `unshare` is denied, rather than skipping.

## 2. Failing tests first

- [ ] 2.1 Add unit tests in `crates/platform-linux-wayland/src/capabilities.rs` for the (not yet existing) pure decision function, one case per spec row: handshake answered with the identity marker → PlatynUI; handshake answered without the marker → not PlatynUI; no socket path configured; connection refused; handshake timeout; `XDG_CURRENT_DESKTOP=platynui` with no handshake → PlatynUI; a foreign desktop value with a foreign peer binary → that compositor; nothing at all → unrecognised; and `XDG_CURRENT_DESKTOP=platynui` *together with* a successful handshake → PlatynUI decided by the handshake. Each case also asserts the recorded basis. Verify the file does not compile yet (the function is missing) — that is the failing state — with `just test-crate platynui-platform-linux-wayland`.
- [ ] 2.2 Add a unit test that `WaylandHighlightProvider::highlight` with one rectangle returns `CapabilityUnavailable` when no PlatynUI compositor is detected, and that `clear()` (and an empty highlight request, which delegates to it at `src/highlight.rs:10-12`) still returns `Ok`. Unit tests see `compositor_type() == None`, the same condition `src/screenshot.rs:207` relies on. Verify the highlight case fails today because the provider returns `Ok(())` (`src/highlight.rs:14-17`).
- [ ] 2.3 Add regression anchors and confirm they pass **before** any change: `WaylandScreenshot::capture` returns `CapabilityUnavailable` naming the capability and the compositor (`src/screenshot.rs:33-36`), and every `WaylandWindowManager` operation returns an error rather than a value when no compositor is detected (`src/window_manager/mod.rs:99-110`).
- [ ] 2.4 Add a compositor-side test in `apps/wayland-compositor/tests/ipc_tests.rs`: the `status` response carries the identity marker, still carries `version`, `backend`, `uptime_secs`, `socket`, `xwayland`, `windows`, `minimized` and `outputs`, and `ping` answers identically; an unknown command still returns an error response. Verify the marker assertion fails today.
- [ ] 2.5 Add an `#[ignore]`d namespace test (driven by the harness from 1.2, in the style of the `#[ignore]`d live tests in `crates/java-agent/tests/live_fixture.rs`: a missing prerequisite — user namespaces blocked, `unshare` absent, the compositor or CLI binary not built — **fails** the test with the prerequisite named and never skips) that runs the CLI from the sibling namespace against the compositor and asserts: `platynui-cli info` reports the PlatynUI compositor, and a top-level window's `@Bounds` carries the window's position rather than the origin. Verify it fails today and record the observed values (measured before: compositor `Unknown`, Frame `@Bounds {0,0,600,500}` against the compositor's `{10,40,600,500}`).
- [ ] 2.6 Extend the same `#[ignore]`d test file with the identified-but-unusable case: sibling namespace, `XDG_CURRENT_DESKTOP=platynui`, `PLATYNUI_CONTROL_SOCKET` pointing at a path nothing listens on. Assert that a window-manager operation, a screenshot and a highlight each fail with a message naming that socket path, and that none of them reports success or claims the compositor is unrecognised. Verify it fails today (highlight reports success; the others blame an unknown compositor).
- [ ] 2.7 Add a failing test in `crates/provider-atspi` for the bounds rule (spec: *A substituted window geometry is visible in the log*; design decision 7). Drive it through an injected `WindowManager` whose `resolve_window` or `bounds` returns an error, one case each, and capture the tracing output of the test with a scoped subscriber — the pattern of `crates/provider-java/src/agent/backend.rs:504` with `tracing-subscriber` as a dev-dependency. Assert:
  - a real top-level still answers with the toolkit's rectangle, unchanged;
  - exactly one `warn`-level record names the node, the failing call and the window manager's error;
  - a second read of the same window, including through a freshly built node for it, adds no second warning;
  - a successful window-manager read for that window re-arms the warning;
  - a second failing top-level gets its own warning;
  - a node inside the window and a grafted popup produce no warning of their own.

  If the node's toolkit extents need a bus, put the rate-limit and message decision behind a pure helper over (node key, window-manager outcome) and test that helper instead, so no display and no bus are needed. Verify with `just test-crate platynui-provider-atspi`: red today, because `resolve_window_manager_bounds` (`src/node.rs:1454-1457`) discards the error with `.ok()` and nothing is logged.
- [ ] 2.8 Extend the 2.5 namespace test with the measured pair for the same rule. From the sibling namespace, once detection works, a top-level's bounds equal the compositor's own geometry for that window, and the captured CLI log contains no substitution warning. In the 2.6 identified-but-unusable setup, the same read still succeeds with the toolkit's rectangle, and the log contains exactly one substitution warning naming the window and the control-socket path. Verify it fails today, recording the observed `{0,0,600,500}` against the compositor's `{10,40,600,500}` and the missing warning.

## 3. Compositor: identify itself over the control socket

- [ ] 3.1 Add the identity marker to `build_status_response` (`apps/wayland-compositor/src/control.rs:819-850`) as an additive field that does not depend on process name, path or version. Verify with the test from 2.4 and by running `platynui-wayland-compositor-ctl status` against a live compositor — its output must be unchanged apart from the new information.
- [ ] 3.2 Document the field in the `status` response table and example (`apps/wayland-compositor/docs/ipc-protocol.md:73-119`), and note there that it is what clients use to tell a PlatynUI compositor from any other control-socket peer. Verify the example JSON matches what a live compositor returns.

## 4. Detection without PID visibility

- [ ] 4.1 Add a handshake helper next to `control_ipc::send_command` that sends the `status` command with its own short timeout (1 s, separate from the 5 s the capability calls use — `src/control_ipc.rs:20-21`) and classifies the outcome as: identified, answered-without-marker, no socket path, connect error, I/O or timeout. Verify with a unit test per outcome against a temporary Unix socket (a listener that answers correctly, one that answers something else, one that accepts and never answers, and a path with no listener).
- [ ] 4.2 Add the pure decision function over (handshake outcome, `XDG_CURRENT_DESKTOP`, peer-process name) returning the compositor type and the basis that decided it, and rewrite `detect_compositor` (`src/capabilities.rs:54-58`) to gather those three inputs and call it in the order from design.md decision 1. Add the `platynui` branch to `detect_via_env` (`:114-136`), and keep `detect_via_peercred` only as an input for foreign compositors, never as a veto. Verify the 2.1 unit tests pass.
- [ ] 4.2a Replace the `rustix::net::sockopt::socket_peercred` call in `detect_via_peercred` (`src/capabilities.rs:67`) with a read of `SO_PEERCRED` through `nix::sys::socket::getsockopt` with `sockopt::PeerCredentials`, treating a PID of `0` as "peer not visible" rather than as an error (design.md, Context: the rustix call is undefined behaviour for that value). Add `nix` with `features = ["socket"]` to the crate's dependencies — the same dependency the compositor gains in `fix-compositor-foreign-pidns-clients` — and drop `net` from the crate's `rustix` features, which only this call used. No `unsafe` block. Add `rustix::net::sockopt::socket_peercred` to `disallowed-methods` in `clippy.toml`, with a reason naming the non-zero `Pid` invariant, next to the `wayland_server::Client::get_credentials` entry that `fix-compositor-foreign-pidns-clients` adds. Verify: `just clippy` is clean across the workspace, `grep -rn "socket_peercred" crates apps` finds no remaining call, the diff adds no `unsafe` to the crate, and the 2.1 unit tests still pass.
- [ ] 4.3 Carry the handshake outcome into the session state next to the compositor type (`src/connection.rs:220-231`), so a later refusal can name the control socket. Verify the existing Wayland unit tests still pass and that nothing outside the crate reads the new field.
- [ ] 4.4 Emit the identification record once in `connect_and_enumerate` (`src/connection.rs:243-253`): identified compositor, deciding mechanism, control-socket path with its handshake outcome, desktop-environment value, peer-credential outcome. Verify by running `platynui-cli --log-level debug info` inside the compositor's namespace and from the sibling namespace with the harness, and confirming both runs print exactly one record naming the mechanism that decided.
- [ ] 4.5 Compare the compositor's reported socket name with the client's `WAYLAND_DISPLAY` and log a warning on mismatch, without letting the mismatch change the decision (design.md decision 3). Verify by pointing the client at a correct control socket while `WAYLAND_DISPLAY` names a different socket: the warning appears and the compositor is still identified.

## 5. Gates that no longer fail open

- [ ] 5.1 Make `WaylandHighlightProvider::highlight` return `CapabilityUnavailable` naming the capability and the compositor when the detected compositor has no highlight backend, keeping `clear()` permissive (`src/highlight.rs:14-17`, `:33-35`). Verify the 2.2 unit test passes and that `platynui-cli highlight` now exits non-zero with that message instead of printing `Highlighted 1 region(s).` — the CLI already propagates the error at `crates/cli/src/commands/highlight.rs:47,59`, so it needs no change.
- [ ] 5.2 Re-run the 2.3 anchors and confirm `screenshot` and `window_manager` still report unavailable with the capability and compositor named, and that an identified-but-unusable session reaches the control-socket error that names the path (`src/control_ipc.rs:16-19`) instead of the unrecognised-compositor message. Verify with the 2.6 namespace test.
- [ ] 5.3 Make the bounds fallback visible without changing it. In `resolve_window_manager_bounds` (`crates/provider-atspi/src/node.rs:1454-1457`), keep the window manager's error from both calls — the window lookup (`resolve_window`, `.ok()?` at `:1444`) and the bounds read (`.ok()` at `:1456`) — long enough to emit the warning from design decision 7, then fall back exactly as today: `resolve_extents` (`:1343-1389`) still answers with the toolkit's extents. Keep the shared `resolve_window` helper's other callers (`resolve_is_active_window`, `resolve_window_state`) unchanged. Hold the set of failing top-levels with the injected window manager, keyed by bus name plus object path, re-armed on a successful read. Leave the other two geometry sources exactly as they are — the parent chain for nodes inside a window, the popup-geometry query for grafted popups — and emit nothing for them. Verify: tests 2.7 and 2.8 pass, `just test-crate platynui-provider-atspi` is green, and no returned rectangle changed (the 2.7 value assertions).
- [ ] 5.4 Confirm the warning stays silent where the window manager works. `provider-atspi` is Linux-only and serves X11 and Wayland — Windows uses the UIA provider and is not affected — so run the X11 lane (`just headless=true test-acceptance-x11`) and check two things: every top-level's `@Bounds` is unchanged, and the run's log carries no substitution warning. The compositor lane gets the same check in 7.5. Verify with `robotcode results` and a search of the lane's log for the warning. A warning in a healthy lane means the trigger or the rate limit is wrong, and is this task's to fix.

## 6. Documentation

- [ ] 6.1 Update the detection description in `dev-docs/platform-linux-wayland.md:686` and its table row at `:720` to describe identification by control-socket handshake with the environment marker and peer credentials as fallbacks, and record that identification must not depend on the compositor's process being visible. Verify the file no longer claims `SO_PEERCRED` is how the compositor type is detected.
- [ ] 6.2 Update the module documentation that still describes the old mechanism: `crates/platform-linux-wayland/src/capabilities.rs:1-6` and `src/lib.rs:12-15,63-67`. Verify with `just doc` that the crate documentation builds and reads correctly.

## 7. Verification

- [ ] 7.1 Run `just check` and `just test`, and confirm the whole workspace is clean — fmt, clippy, ruff, mypy and every Rust test, including the new unit tests and the compositor IPC test.
- [ ] 7.2 Run the namespace tests through the recipe from 1.3 (`--run-ignored ignored-only`) and confirm 2.5, 2.6 and 2.8 now pass: the compositor is identified from the sibling namespace and bounds carry the window position; in the unusable-socket case the gated operations fail by name, and a top-level keeps its fallback rectangle with exactly one warning naming the window and the socket path.
- [ ] 7.3 Verify the input backend in the sibling namespace: with the compositor identified, `input/mod.rs:93-100` now prefers the control socket over EIS, a path never exercised across a namespace boundary. Confirm from the identification and input logs which backend was selected, that a pointer click through it moves the application's counter, and that a failure falls back to EIS instead of hanging.
- [ ] 7.4 Verify the fallbacks: a compositor started with `--no-control-socket` (`apps/wayland-compositor/src/lib.rs:132-134`) is still identified through `XDG_CURRENT_DESKTOP=platynui`, and a session with neither is reported as unrecognised with its capabilities refused by name.
- [ ] 7.5 Run the real acceptance lane `just test-acceptance-compositor` (it rebuilds the native module first) and confirm it is green — same-namespace behaviour, including highlight, must be unchanged — and that its log carries no bounds-substitution warning, because every window manager call succeeds there. Summarize with `just test-summary`.

## 8. Commit

- [ ] 8.1 Commit the compositor-side change as `feat(wayland-compositor): identify the compositor in the status response` (subject ≤ 72 characters), including the IPC protocol documentation, with the trailer:

  ```
  Co-authored-by: Fabian Tsirogiannis <fabian.tsirogiannis@imbus.de>
  ```

- [ ] 8.2 Commit the backend change as `fix(platform-linux-wayland): identify the compositor without peer PIDs` (subject ≤ 72 characters), including the highlight gate, the harness, the tests and the dev-docs update, with the same `Co-authored-by` trailer. Note that the pre-commit hooks gate the whole project, not just the staged files, so every commit must leave the tree lint-clean.
- [ ] 8.3 Commit the bounds warning separately as `fix(provider-atspi): warn when window bounds fall back` (subject ≤ 72 characters), with its tests and the same `Co-authored-by` trailer, and a body naming the measured symptom it makes diagnosable (a top-level reporting `{0,0,600,500}` against the compositor's `{10,40,600,500}`, and the click that reported success and missed) and stating that the fallback value is kept on purpose. It stays a separate commit, although revertability is no longer the argument now that it changes no value. It is a different crate, so folding it into 8.2 would put `provider-atspi` code under a `platform-linux-wayland` scope. And `atspi-process-identity` edits the same `node.rs`, so an isolated commit keeps this diff reviewable and rebasable on its own.
