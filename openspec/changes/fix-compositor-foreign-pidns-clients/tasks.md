# Tasks

## 1. Tests first

- [x] 1.1 Add a unit test module for the peer-identity mapping in `apps/wayland-compositor/src/client.rs`: a positive PID maps to that process, `0` maps to unknown, a negative PID maps to unknown, and a failed credential read maps to unknown. Drive it through the pure mapping function (raw `ucred` fields or an equivalent injected value in, identity out) so no socket is needed. Verify it does not compile yet, and record that as the starting state — the function arrives in 2.1.
- [x] 1.2 Add a serialization test to the existing `#[cfg(test)] mod tests` in `apps/wayland-compositor/src/control.rs:1499`: a window entry, a popup entry and a minimized-window entry built with an unknown identity serialize to JSON containing `"pid":null`, and the rendered JSON of none of them contains `"pid":0`. Covers spec scenario "A response carrying no identified process is still well-formed"; verify with `cargo nextest run -p platynui-wayland-compositor` once it compiles.
- [x] 1.3 Create `apps/wayland-compositor/tests/pidns_tests.rs` with one `#[ignore]`d test and a module doc comment giving the command that runs it. The test:
  - preflights the topology with `unshare --user --map-current-user --pid --fork --mount-proc /bin/true` and, on failure, **fails** with a message naming the missing prerequisite — unprivileged user namespaces blocked (with the `kernel.apparmor_restrict_unprivileged_userns=0` remedy) or `unshare` not installed. A compositor that cannot start, sockets that never appear and a fixture client that cannot connect fail the same way, each named. It never skips: this is the fail-loud rule of `crates/java-agent/tests/live_fixture.rs` and `justfile:403-404`, not the graceful skip of `ipc_tests.rs`, whose existing tests stay as they are (design 6);
  - starts the compositor as `unshare --user --map-current-user --pid --fork --mount-proc --kill-child <compositor binary> --backend headless --socket-name <unique> --timeout 30 --log-level debug`, with `LIBGL_ALWAYS_SOFTWARE=1` and the inherited `XDG_RUNTIME_DIR`, capturing its log;
  - maps a toplevel *and* a popup from the test process itself by reusing `popup_client::open_toplevel_with_popup` (`apps/wayland-compositor/tests/ipc_tests.rs:377-531`) — the test process is outside that PID namespace, which is what makes it unidentifiable;
  - sends `list_windows` (whose response carries both the `windows` and the `minimized` array, `src/control.rs:398-402`), `get_window` by `app_id`, `window_at_point` inside the mapped window, and `list_popups`, and asserts each returns a successful response describing the window.

  Verify it fails on the current code, and record how it fails (expected: the compositor aborts on the first command that reports the window, so the response never arrives).
- [x] 1.4 Extend the 1.3 test with the value and survival assertions from the spec: every `pid` field in those responses is JSON `null` and none is `0`; afterwards `status` still answers and `shutdown` ends the compositor with a successful exit status; the captured log contains exactly one line per client saying the process could not be identified. A real PID instead of `null` means the namespace topology did not take effect — assert, do not skip, so the test cannot pass vacuously.
- [x] 1.5 Confirm the positive control still passes untouched: `ipc_tests.rs:635` asserts the popup's `pid` equals `std::process::id()`. Run `cargo nextest run -p platynui-wayland-compositor --test ipc_tests` on the current code and again after section 3; it must stay green without edits, because it is what proves the nullable contract did not become "always null".
- [x] 1.6 Add a unit test for the exclusion decision to the same `#[cfg(test)] mod tests` in `apps/wayland-compositor/src/control.rs:1499`: a pure function over the window's captured identity and the caller's identity answers "skip" only when both are known and equal, and "keep" when they differ, when the window's identity is unknown, when the caller's is unknown, and when the window's process is a declared XWayland value rather than a captured identity. Covers the positive-match rule of the spec requirement *The window at a point is never the window of the process that asked*; it must not compile until 4.1 introduces the function.
- [x] 1.7 Add an own-window exclusion test to `apps/wayland-compositor/tests/ipc_tests.rs` — this one needs **no** namespace, because the default lane already has the topology the exclusion is about: the test process is both a Wayland client and the control-socket caller. Map a window from the test process with `popup_client::open_toplevel_with_popup` (`:377-531`) and a second window from a separate process with `start_test_app` (`:679`); place them over the same point and raise the test process's own window in front of it with the existing `move_window`, `resize_window` and `focus_window` commands, then assert:
  - `window_at_point` inside the overlap returns the *test app's* window (by `app_id`), not the test process's own;
  - `window_at_point` over a part of the own window that covers nothing else returns a successful response with `window: null`;
  - `list_windows`, `get_window` by `app_id` and `list_popups` still contain the test process's own window, with its own `pid` — the exclusion is the point lookup's alone;
  - a point over the test app's window alone still returns it, so nothing was excluded for the wrong reason.

  Verify it fails against the current code with exactly the symptom from the measurement — the caller's own window comes back — and record that.
- [x] 1.8 Add the "caller is the reference" case to 1.7: a request from a *different* process must still get the frontmost window at that same point, even though the window belongs to the test process. Drive it from a child that writes one JSON line to the control socket and reads the reply (re-exec the test binary with a marker environment variable via `std::env::current_exe()`, or any equivalent child — `platynui-wayland-compositor-ctl` has no `window_at_point` subcommand, verified). What matters is only that the request arrives on a connection whose peer is a different process. Covers the spec scenario *The caller is the reference, not the owner of the frontmost window*.
- [x] 1.9 Extend the 1.3/1.4 namespace test with the inactive case, so the exclusion cannot silently start eating answers there: in that topology the test process is the client *and* the caller and the compositor can resolve neither, so `window_at_point` inside the mapped window SHALL still return that window. Assert it explicitly with a comment naming the reason (exclusion only on a positive match), because this is also the measured limit of the mechanism — on a sibling-namespace control connection the compositor cannot resolve the caller either. Covers the spec scenario *An unidentifiable caller excludes nothing*.
- [x] 1.10 Add a default-lane test for the *identified* half of the diagnosability requirement, which the other tests only cover for the unidentified case: start the compositor with `--log-level debug` and its log redirected to a file (a pipe can fill at that level and block it), map the popup fixture, ask `list_windows` several times, and assert the log carries exactly **one** `Wayland client process identified` line, that it names this process's pid, that it is at `DEBUG`, and that no `could not identify` line appears. Covers the spec scenario *A client in the compositor's own PID namespace is identified* and the "never again while serving requests" clause of the first requirement. Verify with `cargo nextest run -p platynui-wayland-compositor --test ipc_tests`.
- [x] 1.11 Add a test for the two error answers the `window_at_point` documentation promises: a missing coordinate yields `window_at_point requires x and y`, while a non-numeric one fails request parsing and yields `invalid JSON` — `Request.x`/`y` are `Option<f64>`, so it never reaches the command's own check. Verify with `ipc_tests` and against the text of task 5.2.
- [x] 1.12 Strengthen two assertions the audit found unable to fail: in the 1.7 test, assert the `id` of the window `window_at_point` returns equals that window's index in `list_windows` (the stack walk derives the index itself, where the old single-hit lookup searched for it); in the 1.3 namespace test, assert the one warning line is at `WARN` *and* names the PID namespace as the reason (`SO_PEERCRED reported 0`), because a `null` pid and the bare message would hold just as well if the credential read had failed wholesale. Verify with `ipc_tests` and `just test-compositor-pidns`.

## 2. Capture identity when a connection is accepted

- [x] 2.1 `apps/wayland-compositor/src/client.rs`: give `ClientState` a stored peer identity (unknown or a process id) and a constructor that takes the accepted `UnixStream`, reads `SO_PEERCRED` through `nix::sys::socket::getsockopt(&stream, sockopt::PeerCredentials)` — a safe call, no `unsafe` block (design 2) — and maps the result per 1.1. Log once per client: `warn` when the process could not be identified — naming whether the credentials said `0` or the read failed — and `debug` when it could, because the compositor's default log level is `warn` (`src/lib.rs:224-246`). Add `nix` with `features = ["socket"]` (its current release) to the crate's Linux dependencies. Verify with the 1.1 test and `just test-crate platynui-wayland-compositor`, and that the diff adds no `unsafe` to `apps/wayland-compositor`.
- [x] 2.2 `apps/wayland-compositor/src/backend/mod.rs:204-206`: build the client data from the accepted stream before `insert_client` instead of `ClientState::default()`. Verify that `ipc_tests` still passes, including 1.5.
- [x] 2.3 `apps/wayland-compositor/src/handlers/security_context.rs:28-31`: the same for security-context connections, so a sandboxed client is identified at its own accept. Verify by compiling and by `grep -rn "ClientState::default()" apps/wayland-compositor/src` returning no `insert_client` call site.
- [x] 2.4 `apps/wayland-compositor/src/control.rs`: capture the caller's identity where a control connection is accepted — `register_control_client` (`:285`) holds the accepted `UnixStream` — with the same mapping helper as 2.1, store it on `ControlClient` (`:219-228`) and pass it into `process_command` (`:382`), which today receives only the request text and the state. Log it once per control connection at `debug` in **both** outcomes: the runtime opens one connection per command (`crates/platform-linux-wayland/src/control_ipc.rs:10-21`), so a `warn` here would flood a log that the Wayland-side `warn` from 2.1 already makes readable. Verify by compiling and by `ipc_tests` staying green (the caller is not used for anything yet).

## 3. Use the stored identity everywhere

- [x] 3.1 `apps/wayland-compositor/src/control.rs:1270-1278`: `window_pid` keeps returning `x11.pid()` for XWayland-backed windows and otherwise reads the stored identity via `client.get_data::<ClientState>()` (the pattern at `src/handlers/compositor.rs:27`), returning unknown when there is no such data — which is the case for XWayland's own connection. It must not call `client.get_credentials` any more. Verify with the 1.3/1.4 test, which now passes.
- [x] 3.2 `apps/wayland-compositor/src/security.rs:90-112`: `is_client_allowed` reads the stored identity instead of `client.get_credentials`, and splits the policy decision into a function over `Option<u32>` so it is unit-testable. Add unit tests for the two spec scenarios: unknown identity under a restrictive policy is denied, and any identity under a permissive policy is allowed. Verify with `just test-crate platynui-wayland-compositor`.
- [x] 3.3 Add `{ path = "wayland_server::Client::get_credentials", reason = "…" }` to a new `disallowed-methods` list in `clippy.toml` and `disallowed_methods = "deny"` to `[workspace.lints.clippy]` in the root `Cargo.toml`, next to the existing `disallowed_types = "deny"`. Do **not** add `rustix::net::sockopt::socket_peercred` — `crates/platform-linux-wayland/src/capabilities.rs:67` still calls it and `just clippy` is workspace-wide with `-D warnings` (`justfile:171-173`); that entry belongs to `wayland-sidecar-capabilities`. Verify with `just clippy`: it must pass, and re-adding a `get_credentials` call must make it fail.

- [x] 3.4 `apps/wayland-compositor/src/control.rs`: normalize a declared `_NET_WM_PID` of `0` to unknown where `WindowOwner::Declared` is built, so the never-`0` requirement holds for the XWayland source too — smithay stores whatever `CARDINAL` the client wrote (`smithay-0.7.0/src/xwayland/xwm/surface.rs:767`), and nothing else filters it. Keep it a pure function so it is unit-testable without an X11 window, and add the unit test. Verify with `just test-crate platynui-wayland-compositor`.

## 4. Never answer a point with the caller's own window

- [x] 4.1 Add the pure exclusion decision from 1.6 next to `window_pid` in `apps/wayland-compositor/src/control.rs`: given the window's identity (the captured one, or "declared only" for an XWayland window) and the caller's identity, it answers "skip" only when both are known and equal. Its doc comment states why a declared `_NET_WM_PID` is not an identity — the compositor did not establish it and an X11 client could otherwise make its window unpickable by claiming the caller's number. Verify the 1.6 tests pass.
- [x] 4.2 Rewrite `window_at_point` (`apps/wayland-compositor/src/control.rs:565-579`) to walk the stack instead of taking `Space::element_under`'s single answer: `state.space.elements()` reversed — front-to-back, the order the renderer relies on (`src/render.rs:114-115`) — with the same hit criterion smithay applies (bounding box contains the point, then the window's input region at its render location, `smithay-0.7.0/src/desktop/space/mod.rs:185-200`), returning the first hit the 4.1 decision does not exclude and `window: null` when none is left. Keep deriving the reported `id` from the returned window's position (`:572`). Verify with 1.6/1.7/1.8, and verify against the "nothing excluded" case that the walk picks exactly the window `element_under` would have — if the render location cannot be reconstructed through the public `Space` API (design 9's one assumption), use the accessor that does before changing the criterion.
- [x] 4.3 Confirm the exclusion has exactly one call site: `grep -rn` the decision function in `apps/wayland-compositor/src` returns `window_at_point` only, so `list_windows`, `get_window`, `list_popups` and the minimized listings keep reporting the caller's own windows. Verify with 1.7's listing assertions (spec scenario *The caller's own window is still listed*).

## 5. Documentation and tooling

- [x] 5.1 `apps/wayland-compositor/docs/ipc-protocol.md`: document the `pid` field of window, minimized-window, popup and `window_at_point` responses as nullable — `null` means the compositor could not identify the client's process (a client in a PID namespace it cannot see), `0` is never reported, and for an XWayland window the value is the X11 client's declared `_NET_WM_PID` rather than peer credentials. Verify by comparing the text against the three spec requirements it covers.
- [x] 5.2 `apps/wayland-compositor/docs/ipc-protocol.md`: add the `window_at_point` section the document does not have today (verified: the command appears in none of its headings), describing the request, the response and the exclusion rule — the frontmost window at the point that the *asking* process does not own, `window: null` when there is none, and no exclusion when either identity is unknown or the window's process is only an XWayland client's declared value. Verify the text against the spec requirement *The window at a point is never the window of the process that asked*.
- [x] 5.3 Add a `just` recipe that runs the ignored namespace test (e.g. `test-compositor-pidns` → `cargo nextest run -p platynui-wayland-compositor --run-ignored ignored-only -E 'binary(pidns_tests)'`, the pattern used by the Java-agent live recipes at `justfile:400` (Windows) and `justfile:407` (Unix)), with a comment stating that it needs unprivileged user namespaces, that a missing prerequisite fails the run rather than skipping the coverage (the wording of the `test-java-agent-live` comment), and that it is deliberately not in `just test`. Verify by running the recipe, and once with user namespaces unavailable (a machine that restricts them, or temporarily `sysctl -w user.max_user_namespaces=0`) to confirm it exits non-zero with the prerequisite named.

## 6. Verification

- [x] 6.1 Run `just test-crate platynui-wayland-compositor` — the 1.1, 1.2, 1.6 and 3.2 unit tests pass and the 1.7/1.8 `ipc_tests` cases pass, with 1.5 green and unedited.
- [x] 6.2 Run the 5.3 recipe — the namespace test passes; then re-run it with the section 2/3 changes stashed to confirm it still reproduces the abort, so the test is known to be the guard and not a tautology.
- [x] 6.3 Run `just check` (fmt, clippy `-D warnings`, ruff, mypy) — clean, and in particular `just clippy` is clean *with* the new ban in place.
- [x] 6.4 Run the compositor acceptance lane as a regression check on real sessions where every client *is* identifiable: `just headless=true test-acceptance-compositor`, judged with `uv run --no-sync robotcode results summary --failed`; the lane exits `0` with no new failures. In that lane the exclusion is *active* (one namespace, an identifiable caller), so also spot-check a pick over the runtime's own window: it must resolve the window behind it, or nothing, and never the runtime's own window.
- [x] 6.5 Keep durable coverage for the consumer behaviour the exclusion exists for, which 6.4's spot check only proved once: add `tests/acceptance/egui/inspector_picker_own_window.robot` (tagged `platform:wayland`, the compositor owns this exclusion; the X11 half belongs to `x11-window-owner-identity`), laying the Inspector's own window OVER the test app's button and asserting the live pick selects that button. It is the only guard left once `atspi-process-identity` drops the provider's own-process filter. Verify with `just headless=true test-acceptance-compositor` — and once with `apps/wayland-compositor/src/control.rs` reverted, where it must fail with the measured symptom (the picker selects nothing at all).

## 7. Commit

- [x] 7.1 Commit the change as `fix(compositor): survive clients from foreign PID namespaces` (Conventional Commits, subject ≤ 72 characters), with a body naming the abort it removes, the `null`-not-`0` contract and the own-window exclusion in `window_at_point` (identity established by the compositor on both sides, exclusion only on a positive match), and the trailer:

  ```
  Co-authored-by: Fabian Tsirogiannis <fabian.tsirogiannis@imbus.de>
  ```

  Verify with `git log -1 --format=%B` that the trailer is present and the subject follows the convention. Do not push.
