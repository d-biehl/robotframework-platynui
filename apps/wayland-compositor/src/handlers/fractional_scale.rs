//! `wp-fractional-scale-v1` handler — high-DPI rendering support.
//!
//! Allows clients to render at fractional scale factors (e.g., 1.25x, 1.5x).
//! The `FractionalScaleHandler` trait implementation lives in `state.rs`
//! alongside the other handler traits that need direct `State` access.
