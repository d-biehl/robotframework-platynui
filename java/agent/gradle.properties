# Cache the configuration phase between invocations (and silence Gradle's
# "consider enabling" hint); the build scripts are configuration-cache clean.
org.gradle.configuration-cache=true

# The agent version. It is handed to the provider in the handshake file and
# compared for EXACT equality (design 1a) — so it must stay in lockstep with the
# workspace version in Cargo.toml / pyproject.toml, exactly like the other
# per-package version literals in this repo.
version=0.13.0-dev.2
